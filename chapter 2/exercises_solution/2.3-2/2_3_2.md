when we use $p \neq r$ instead of $p \geqslant r$

if we pass the value of $n$ such that $n \geqslant 1$

then for any arbitrary function call sort($A, p, r$)

$q = \left\lfloor \frac{p+r}{2} \right\rfloor$ will always be between $p$ \& $r$

as due to AM $p \leqslant \frac{p+r}{2} < r$

so the worst case when $q=p$ ($r=p$)
exits the loop due to "if $p \neq r$"
condition

the edge case when $r = p+1$
would result in $q = \left\lfloor \frac{p+1+p}{2} \right\rfloor = p$

\& hence $p > r$ never happens.
